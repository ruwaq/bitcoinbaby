[package]
name = "babtc-contract"
version = "0.1.0"
edition = "2021"
description = "BABTC Token Contract for BitcoinBaby mining rewards"
license = "MIT"

[dependencies]
charms-sdk = { version = "11.0.1" }
serde = { version = "1.0", features = ["derive"] }
sha2 = { version = "0.10.9" }

[profile.release]
lto = "fat"
codegen-units = 1
strip = "symbols"
panic = "abort"
overflow-checks = true

# Empty [workspace] section to make this a "top-level" project
[workspace]
